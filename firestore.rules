rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Izinkan user terautentikasi membaca dan menulis semua collection
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
